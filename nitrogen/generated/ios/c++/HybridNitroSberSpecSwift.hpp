///
/// HybridNitroSberSpecSwift.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include "HybridNitroSberSpec.hpp"

// Forward declaration of `HybridNitroSberSpec_cxx` to properly resolve imports.
namespace NitroSber { class HybridNitroSberSpec_cxx; }

// Forward declaration of `SID_STAND` to properly resolve imports.
namespace margelo::nitro::nitrosber { enum class SID_STAND; }

#include <NitroModules/Promise.hpp>
#include <string>
#include <optional>
#include "SID_STAND.hpp"

#include "NitroSber-Swift-Cxx-Umbrella.hpp"

namespace margelo::nitro::nitrosber {

  /**
   * The C++ part of HybridNitroSberSpec_cxx.swift.
   *
   * HybridNitroSberSpecSwift (C++) accesses HybridNitroSberSpec_cxx (Swift), and might
   * contain some additional bridging code for C++ <> Swift interop.
   *
   * Since this obviously introduces an overhead, I hope at some point in
   * the future, HybridNitroSberSpec_cxx can directly inherit from the C++ class HybridNitroSberSpec
   * to simplify the whole structure and memory management.
   */
  class HybridNitroSberSpecSwift: public virtual HybridNitroSberSpec {
  public:
    // Constructor from a Swift instance
    explicit HybridNitroSberSpecSwift(const NitroSber::HybridNitroSberSpec_cxx& swiftPart):
      HybridObject(HybridNitroSberSpec::TAG),
      _swiftPart(swiftPart) { }

  public:
    // Get the Swift part
    inline NitroSber::HybridNitroSberSpec_cxx& getSwiftPart() noexcept {
      return _swiftPart;
    }

  public:
    inline size_t getExternalMemorySize() noexcept override {
      return _swiftPart.getMemorySize();
    }
    bool equals(const std::shared_ptr<HybridObject>& other) override {
      if (auto otherCast = std::dynamic_pointer_cast<HybridNitroSberSpecSwift>(other)) {
        return _swiftPart.equals(otherCast->_swiftPart);
      }
      return false;
    }
    void dispose() noexcept override {
      _swiftPart.dispose();
    }
    std::string toString() override {
      return _swiftPart.toString();
    }

  public:
    // Properties
    

  public:
    // Methods
    inline void closeAuthorizationViewControllers() override {
      auto __result = _swiftPart.closeAuthorizationViewControllers();
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
    }
    inline std::shared_ptr<Promise<void>> auth(const std::string& scope, const std::string& state, const std::string& nonce, const std::string& redirectUri, const std::optional<std::string>& ssoBaseUrl, const std::optional<std::string>& codeChallenge, const std::optional<std::string>& codeChallengeMethod, const std::optional<std::string>& loginHint) override {
      auto __result = _swiftPart.auth(scope, state, nonce, redirectUri, ssoBaseUrl, codeChallenge, codeChallengeMethod, loginHint);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }
    inline std::shared_ptr<Promise<void>> initialize(const std::string& clientId, const std::optional<std::string>& partnerName, const std::optional<std::string>& userID, const std::optional<std::string>& themeColorLight, const std::optional<std::string>& themeColorDark, const std::optional<std::string>& partnerProfileUrl, std::optional<bool> isShowErrorOnMain, std::optional<SID_STAND> standType) override {
      auto __result = _swiftPart.initialize(clientId, partnerName, userID, themeColorLight, themeColorDark, partnerProfileUrl, isShowErrorOnMain, standType);
      if (__result.hasError()) [[unlikely]] {
        std::rethrow_exception(__result.error());
      }
      auto __value = std::move(__result.value());
      return __value;
    }

  private:
    NitroSber::HybridNitroSberSpec_cxx _swiftPart;
  };

} // namespace margelo::nitro::nitrosber
